1) We are getting confusion w.r.t Motor on/off status and Runtime
2) Currently we are dealing with live data last sync from next data sync is run time though signal got lost.
3) We need to update this logic of Motor on/off status and run time with handling device online or offline